{
  "buildCommand": "pnpm install && pnpm build --filter @media-sdk/web-app",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web-app/dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web-app"
}
